{
  // Root config that references specific environment configs
  // This split configuration pattern enables:
  // 1. Different settings for browser vs Node.js environments
  // 2. Better build performance through focused file processing
  // 3. Clear boundaries to prevent API misuse between environments
  // 4. Separate build caching for faster incremental builds
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
